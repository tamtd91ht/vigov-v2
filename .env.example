# ViGov — environment template.
#
# PLACEHOLDERS ONLY. A real value here reaches git the moment someone forgets, and a secret in
# git cannot be recalled: the history is already on every clone. Where one process serves 200+
# communes, one leaked signing key affects every commune, not one (rule 8).
#
#   cp .env.example .env     then fill .env, which .gitignore already blocks
#
# ONE FILE FOR ALL EIGHT SERVICES, on purpose. core/config.Load(serviceName) reads the same
# platform-wide constants everywhere, so a template per service would be eight copies of one
# fact — and the copy nobody updates is the one the next person follows (rule 9).
#
# WHAT DOES NOT BELONG HERE: anything that differs per commune — name, upper authority, logo,
# coordinates, SLA figures, working hours, public holidays, duty phone. Those are read AT
# RUNTIME from per-tenant configuration in the database. An environment variable is a constant
# of the process, and one process serves every commune (rule 1, invariant 10).
#
# Each variable is documented where it is read: core/config/config.go.

# ---- required ----------------------------------------------------------------

# This service's OWN schema. Never another service's (rule 2, forbidden #2).
DATABASE_DSN=postgres://<user>:<password>@<host>:5432/<database>?sslmode=require

# dev | staging | prod. Decides nothing about business behaviour — only how strict startup is.
ENV=dev

# Signing keys for the session token, newest FIRST.
#
# A LIST, not one key: the first signs, every entry still verifies. That is what makes rotation
# possible — with a single key, rotating it signs every open session out, across every commune
# at once (rule 8, invariant 6). Rotate by prepending the new key, then dropping the last one a
# session lifetime later.
#
#   generate:  openssl rand -base64 32
#
SESSION_SIGNING_KEYS=<new-key>,<previous-key>

# ---- optional ----------------------------------------------------------------

# Duplicate-request protection and rate limiting. Empty outside dev is reported at every
# startup: with no Redis there is nothing standing between a double-submitted POST and a second
# permanent record.
REDIS_DSN=redis://<host>:6379/0

# Default :8080, the same for EVERY service — under k8s each pod has its own IP, and the
# Service, the probes and the NetworkPolicy all assume 8080. Leave it empty there.
# Running several services on ONE machine: set it per process, e.g. identity :8080,
# platform :8081, documents :8083, petitions :8084, finance :8086, comms :8087,
# reporting :8088. These are suggestions for a local run, not defaults the code knows.
LISTEN_ADDR=

# Inter-service gRPC. Default :9090 — a SEPARATE port from LISTEN_ADDR because gRPC needs
# HTTP/2 while the REST surface is served to browsers over HTTP/1.1. This port is internal:
# it belongs behind the cluster boundary and must not be published to the internet.
GRPC_LISTEN_ADDR=

# ---- required, every service, every environment ---------------------------------

# The caller key for the inter-service gRPC port. ONE value, the SAME value in every service on
# the deployment — not one per service, and no key id. From a k8s secret, never from this file
# with a real value in it.
#
# TWO LAYERS, AND THIS IS ONLY THE FIRST. The other is the gRPC port staying inside the
# cluster's internal network, unpublished. This key stops a process that reached the port; the
# network policy is what stops it reaching the port at all. Whoever removes one of them is
# removing half of the whole protection, not one measure among several (ADR 0025).
#
# WHAT IT DOES NOT BUY, so nobody assumes more: it does not say WHICH service called, so an
# inter-service call cannot be attributed to a caller; anyone inside the cluster holding it can
# call anything; and because there is exactly one, ROTATING IT MEANS CHANGING IT EVERYWHERE AT
# ONCE — a coordinated deployment, not a per-service action (rule 8, invariant 6). A rolling
# restart with half the fleet on the new value is a fleet refusing half its own calls.
#
# REQUIRED IN EVERY ENVIRONMENT, DEV INCLUDED. A service without it refuses to start and names
# this variable. No dev exemption, unlike SESSION_SIGNING_KEYS: a missing signing key means this
# process cannot read a session — local and visible — while a missing caller key means a gRPC
# port that accepts whatever reaches it, and "worked fine locally" is how that reaches a cluster.
#
#   generate:  openssl rand -base64 32
#
GRPC_CALLER_KEY=<dien-vao-tu-k8s-secret>

# ---- required for every service EXCEPT platform --------------------------------

# Where the platform service answers ResolveHost — the RPC that maps an incoming Host to a
# commune. Every service edge resolves its commune through this address before any handler runs.
#
# NO DEFAULT, on purpose: guessing the address of the registry is a default on the isolation
# path (rule 1, forbidden #1). A service that needs it and does not have it refuses to start and
# says so by name, rather than answering 404 for every domain — which reads like a DNS problem
# and sends somebody looking in the wrong place for an afternoon.
#
# The platform service itself leaves this empty: it holds the registry and calls nobody.
# Cluster-internal address only. There is no mTLS and no per-service identity on this hop: what
# guards it is GRPC_CALLER_KEY below plus the port staying off the public network — both, not
# either.
PLATFORM_GRPC_ADDR=platform:9090

# ---- required for every service that guards a STAFF route ----------------------

# Where the identity service answers ResolveStaffPrincipal — the RPC that turns a staff member's
# session cookie into the principal every guard needs. Without it, a guarded route in documents,
# finance, comms or petitions answers 401 to everybody, including a member of staff holding a
# perfectly valid session.
#
# NO DEFAULT, exactly like PLATFORM_GRPC_ADDR and for the same reason. A guessed address does not
# fail closed in a way anyone can read: every staff request answers 503 while identity is healthy,
# which looks like an outage of the wrong service.
#
# Two services leave it empty: identity itself builds its principal from its own session registry,
# and platform holds the registry and calls nobody.
#
# CLUSTER-INTERNAL ADDRESS ONLY, and the stakes are higher here than on the platform hop: what
# travels on it is a WORKING SESSION TOKEN, and there is no TLS and no per-service identity on this
# boundary (ADR 0025). An address that leaves the cluster puts every staff session on the wire in
# the clear.
IDENTITY_GRPC_ADDR=identity:9090

# How long a deactivated commune keeps being served from cache. Default 30s, short on purpose.
#
# NOT DECLARED IN THE CLUSTER, deliberately, since 2026-09-23: both environments want the same
# number, so a ConfigMap entry would be the default copied to a second place — and the second
# place is the one that drifts. This line stays because rule 11 invariant 6 requires every
# variable `core/config` reads to be discoverable here, not because anything sets it.
TENANT_CACHE_TTL=

# Hops allowed to state the client address through X-Forwarded-For — k8s ConfigMap, not Secret.
# Comma-separated CIDRs or bare IPs (a bare IP is one host), e.g. the pod CIDR that ingress-nginx
# and web-admin run in: 10.0.0.0/8, 192.0.2.10. The value belongs to whoever operates the cluster.
#
# EMPTY TRUSTS NOBODY: the audit trail records this process's own socket peer, which behind
# web-admin is web-admin's pod IP — wrong, but never forged. A malformed entry, 0.0.0.0/0 or ::/0
# stops the service at startup, by name: trusting everybody is trusting a forged header.
TRUSTED_PROXY_CIDRS=

# Browser origins allowed to call the CITIZEN edge cross-origin (the Zalo Mini App webview) —
# k8s ConfigMap, not Secret. Comma-separated: exact https origins, or `https://*.<domain>` (at
# least one label before the domain, no port). CITIZEN ROUTES ONLY — staff routes never send CORS.
#
# EMPTY = NO CORS HEADERS: the browser blocks the Mini App (fail closed). `*`, any http:// entry,
# a path or trailing slash stops the service at startup, by name. No Access-Control-Allow-
# Credentials is ever sent: the citizen edge uses bearer tokens, not cookies.
#
# Recommended production value (h5.zdn.vn + zalo.me are what vihat-miniapp already allows, see
# that repository's README §CORS; the two *. entries are the owner's approved loosening, 2026-09-26):
#   https://h5.zdn.vn,https://zalo.me,https://*.zdn.vn,https://*.zalo.me
CITIZEN_CORS_ALLOWED_ORIGINS=

# ---- citizen-session bridge — service-identity only (ADR 0045) -----------------
#
# The second gRPC listener of identity, serving CitizenSessionBridgeService and NOTHING else. Its
# only caller is the vihat-miniapp backend, inside the same cluster, admitted by a NetworkPolicy
# that lets only those pods reach this port. Never published, never behind ingress.
#
# BOTH OR NEITHER. Both empty: the bridge is not started and identity still serves staff. One set
# without the other: identity refuses to start and names the missing one.
#
# k8s ConfigMap. No default: the port must match the NetworkPolicy rule, so it is written in the
# manifest and nowhere else.
CITIZEN_SESSION_BRIDGE_LISTEN_ADDR=

# k8s SECRET. Comma-separated list — ANY entry is accepted, vihat-miniapp sends one. A list so the
# two repositories can rotate without releasing at the same instant: add the new key, switch
# vihat-miniapp, then drop the old one. NOT GRPC_CALLER_KEY, and never the same value as it: that
# key opens every RPC of every service (ADR 0025). Each key at least 32 bytes.
#
#   generate:  openssl rand -base64 32
#
CITIZEN_SESSION_BRIDGE_KEYS=<dien-vao-tu-k8s-secret>,<khoa-cu-trong-luc-xoay>

# k8s ConfigMap. Lifetime of a citizen session issued through the bridge. Go duration syntax.
# Empty = 720h (30 days), the owner's decision of 2026-09-25. Malformed or <= 0 stops the service.
CITIZEN_SESSION_TTL=

# ---- identity: the default administrator of a commune ---------------------------
#
# k8s SECRET `bi-mat-identity`, identity only. The password of the account identity creates in a
# commune at that commune's FIRST sign-in as `admin` — created only when the commune has no
# `admin` row in any state, and created with "must change password" set, so the value typed here
# stops working for that commune at its first sign-in.
#
# EMPTY = OFF, and off is the expected state once every commune has changed its admin password:
# remove the key then. Shorter than 12 characters = also off, reported at identity startup.
# Never a real value here — the template holds a placeholder only (rule 8).
IDENTITY_ADMIN_SEED_PASSWORD=<dien-vao-tu-k8s-secret>

# ---- infrastructure: RabbitMQ and Elasticsearch, none of it required yet -------
#
# WHICH k8s OBJECT SUPPLIES WHICH VALUE IS NOT A DEPLOYMENT DETAIL — it is the difference
# between a value that may sit in a git-tracked manifest and one that may not (rule 8):
#
#   k8s Secret     anything credentialed — a password, a token, an API key, or a DSN with a
#                  password inside it. Held in Go as secret.Secret / secret.DSN, which refuse
#                  to render on every printing path
#   k8s ConfigMap  anything not credentialed — a host, a port, an address list, an exchange
#                  name, an index prefix
#
# The full table, every variable in this file with its object and its refusal, is in the package
# doc of core/config/config.go. It lives there and not here because that is the file that reads
# them, and a classification the config layer cannot check is one that drifts.
#
# NONE OF THE FIVE BELOW IS REQUIRED, and that is deliberate. Nothing in this repository
# connects to RabbitMQ or Elasticsearch yet. A required variable here would stop all eight
# services from starting on every machine that has not set four more values, in order to protect
# code that does not exist. Each is refused BY NAME by the first thing that genuinely needs it —
# the same shape as IDENTITY_GRPC_ADDR above.

# k8s SECRET. Background work with a DELAY: deadline reminders, escalation, the five automation
# jobs. NEVER inter-service events — those are Kafka's, and putting one here breaks rule 2,
# invariant 3 (ADR 0010).
RABBITMQ_DSN=amqp://<user>:<password>@<host>:5672/<vhost>

# k8s ConfigMap. The exchange the background-task publisher binds to.
#
# LEFT EMPTY ON PURPOSE, and not out of laziness: ADR 0010 chose RabbitMQ and said what it is
# for, but named no exchange, queue or routing key. A plausible name written here would record
# that decision in a template instead of in an ADR, and the next reader would take it as settled.
# Whoever wires the first job names it.
RABBITMQ_EXCHANGE=

# k8s ConfigMap. Search cluster nodes, comma-separated.
#
# FULL-SYSTEM SEARCH ONLY. Elasticsearch must never produce a reported figure: it is
# near-real-time, and disbursement totals and overdue-petition counts are numbers that go to
# leadership — one that is off is an incident somebody answers for, not a display bug. Reporting
# figures come from the PostgreSQL read model (ADR 0010).
#
# ADR 0010 also says to CONSIDER DEFERRING Elastic entirely at this stage: pg_trgm + unaccent is
# probably enough at ~10,000 records per commune, and it should be added when search is measured
# slow rather than assumed so. Empty is the expected value today.
ELASTICSEARCH_ADDRS=http://<host>:9200,http://<host>:9200

# k8s SECRET. Authentication to the search cluster.
#
# Empty while ELASTICSEARCH_ADDRS is set is reported at every startup outside dev: an index
# holding petition contents is an index holding citizen personal data (rule 3), and a cluster
# reachable without authentication is a choice somebody has to own rather than inherit.
ELASTICSEARCH_API_KEY=<dien-vao-tu-k8s-secret>

# k8s ConfigMap. Namespaces this DEPLOYMENT's indices — nothing more.
#
# The per-commune part of an index name is NOT this variable. Rule 1, invariant 7 requires
# t:<tenant_id> on every index, room, cache key and queue, and that belongs to the indexer where
# the commune is in the context. One process serves every commune, so a commune's name in an
# environment variable gives every commune whichever one was deployed last (rule 8, invariant 5).
ELASTICSEARCH_INDEX_PREFIX=

# ---- dangerous — must be off before production --------------------------------

# Disables authentication. Local development only; leave EMPTY unless you mean it.
#
# Reported as a warning at EVERY startup, and refused outright when ENV=prod: a flag switched on
# for one local afternoon is forgotten by the next morning (rule 8, invariant 7).
DANGEROUS_AUTH_BYPASS=

# ---- tests --------------------------------------------------------------------

# Integration tests are SKIPPED when this is unset, and `go test ./...` still passes. Point it
# at a test database only — each run creates its own schema and drops it afterwards.
#
# ⚠ THE SKIP IS SILENT, AND IT COST A WHOLE NIGHT ON 22-23/09/2026. Six sessions in a row wrote
# "VIGOV_TEST_DSN trống → ca pg bỏ qua" into their reports and moved on, while a PostgreSQL 16
# container had been running on this machine the entire time. Nobody looked, because a skipped
# suite prints `ok` — the same word a suite that ran prints.
#
# HOW TO TELL THE TWO APART WITHOUT TRUSTING A WORD: look at the DURATION. `internal/store` of
# service-identity takes ~0.02s when it skips and ~6s when it runs. A store suite finishing in
# hundredths of a second has not opened a connection.
#
# The container used on 23/09/2026, for reference only — it is a local development container and
# nothing in the repository depends on it existing:
#
#	docker run -d --name vigov-schema-test -p 55432:5432 \
#	  -e POSTGRES_USER=vigov -e POSTGRES_HOST_AUTH_METHOD=trust postgres:16
#
#	VIGOV_TEST_DSN="postgres://vigov@127.0.0.1:55432/postgres?sslmode=disable"
#
# `go run ./tools/schema-smoke` is the one command that FAILS instead of skipping when it cannot
# connect — run it first, before believing any suite that printed `ok`.
VIGOV_TEST_DSN=
