# Rút một node để vá hệ điều hành không được làm đứt phân giải xã của toàn hệ thống.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: platform
spec:
  minAvailable: 2
  selector:
    matchLabels: { app.kubernetes.io/name: platform }
